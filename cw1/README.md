# CourseWork part 1: Lexer

## Compilation: 
```bash
git clone --recurse-submodules https://github.com/BorisVassilev1/FMI-Compilers.git
./tools/build.sh -t lexer
```

## Usage:
```bash
./build/lexer
```
This will run the lexer on stdin and output the tokens to stdout.
